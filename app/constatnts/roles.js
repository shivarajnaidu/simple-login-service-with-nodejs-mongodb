'use strict';

const USER = 'USER';
const ADMIN = 'ADMIN';
const SUPER_ADMIN = 'SUPER_ADMIN';

const roles = [
  USER,
  ADMIN,
  SUPER_ADMIN,
];

module.exports = {
  USER,
  ADMIN,
  SUPER_ADMIN,
  roles,
};
